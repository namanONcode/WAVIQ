add_executable(module1_tests test_module1.cpp)
target_link_libraries(module1_tests PRIVATE module1_core)
target_compile_definitions(module1_tests PRIVATE TESTDATA_DIR="${PROJECT_SOURCE_DIR}/testdata")
add_test(NAME module1_tests COMMAND module1_tests)

if(TARGET Qt6::Test OR TARGET Qt5::Test)
    if(Qt6_FOUND)
        set(QT_TEST_TARGET Qt6::Test)
        set(QT_WIDGETS_TARGET Qt6::Widgets)
    else()
        set(QT_TEST_TARGET Qt5::Test)
        set(QT_WIDGETS_TARGET Qt5::Widgets)
    endif()
    add_executable(module1_gui_tests test_gui.cpp ../src/QtVisualizationView.cpp ../include/module1/QtVisualizationView.h)
    target_link_libraries(module1_gui_tests PRIVATE module1_core ${QT_WIDGETS_TARGET} ${QT_TEST_TARGET})
    target_compile_definitions(module1_gui_tests PRIVATE TESTDATA_DIR="${PROJECT_SOURCE_DIR}/testdata")
    add_test(NAME module1_gui_tests COMMAND module1_gui_tests)
    set_tests_properties(module1_gui_tests PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen")
endif()
